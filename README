Added patch are:

noborder				-> no border in zoom mode or when only one window in the tag
fakefullscreen	-> when you full screen youtube, only take up it's own panel
center					-> set a switch whether a new window is to be opened at center
alttag					-> add pretty tag instead of numbers and toggle with Mod+n
systray					-> light system tray right on the topbar
pertag					-> resize and mode of panel per tag (not global)
attachbelow			-> instead of alwaysmaking the master
